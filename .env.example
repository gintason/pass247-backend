# ---------------------------------------------------------------------------
# PAS — environment template
#
# Copy to .env in the PROJECT ROOT (next to manage.py) and fill in real
# values. settings.py:18 reads BASE_DIR / ".env" — nowhere else.
#
#     cp .env.example .env
#
# Never commit the real .env: it holds live Paystack and Cloudinary keys.
# NOTE: this file must NOT contain DJANGO_ORIGIN — that is a Vite variable
# and belongs in frontend/.env.
# ---------------------------------------------------------------------------

# Django core. Generate a key with:
#   python3 -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# Changing it invalidates all sessions and pending password-reset links.
DJANGO_SECRET_KEY=
DEBUG=True

# Cloudinary — media storage (console.cloudinary.com > Dashboard)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Paystack — dashboard.paystack.com > Settings > API Keys & Webhooks.
# Set the webhook URL there to: https://<your-domain>/api/payments/webhook/
# Use sk_test_/pk_test_ keys in development.
# The two URLs below are Paystack's standard endpoints; leave as-is.
PAYSTACK_LIVE_SECRET_KEY=
PAYSTACK_LIVE_PUBLIC_KEY=
PAYSTACK_INITIALIZE_PAYMENT_URL=https://api.paystack.co/transaction/initialize
PAYSTACK_VERIFY_URL=https://api.paystack.co/transaction/verify/

# Email — used for signup OTP codes and password resets.
# With DEBUG=True Django uses the console backend and prints emails to the
# terminal, so this can stay blank in development.
EMAIL_HOST_PASSWORD=

# Database. Currently unused: settings.py runs SQLite and the MySQL block
# that reads these is commented out. Kept for the eventual move off SQLite.
EMAIL_HOST_USER=noreply@pas.com.ng
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# ---------------------------------------------------------------------------
# PRODUCTION (Render) — set these in the Render dashboard, not in a file.
# Dashboard variables override anything in .env, which is what you want.
# ---------------------------------------------------------------------------

# MUST be False in production. Turning it on enables SSL redirect, HSTS and
# secure-only cookies via the conditional block in settings.py.
# DEBUG=False

# Postgres connection string. Render > your database > "Internal Database URL".
# WITHOUT THIS the app falls back to SQLite, whose file is destroyed on every
# deploy and restart — taking all users, payments and questions with it.
# DATABASE_URL=postgresql://user:pass@host/dbname

# Comma-separated, no spaces. Render's own hostname is added automatically
# from RENDER_EXTERNAL_HOSTNAME, so list only your custom domains here.
# ALLOWED_HOSTS=pass247.net,www.pass247.net
# CSRF_TRUSTED_ORIGINS=https://pass247.net,https://www.pass247.net
# CORS_ALLOWED_ORIGINS=https://pass247.net,https://www.pass247.net

# Where password-reset links point. Must be the FRONTEND's public URL.
# FRONTEND_URL=https://pass247.net
